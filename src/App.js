import './App.css';
import Counter from "./components/Counter/Counter";

const App = () => {
    return (
        <div id={'wrapper'} className={'wrapper'}>
            <Counter/>
        </div>
    );
};

export default App;
